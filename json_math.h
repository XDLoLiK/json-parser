#ifndef JSON_MATH_H
#define JSON_MATH_H

#define MAX(first, second) (((first) > (second)) ? (first) : (second))
#define MIN(first, second) (((first) < (second)) ? (first) : (second))

#endif // JSON_MATH_H
